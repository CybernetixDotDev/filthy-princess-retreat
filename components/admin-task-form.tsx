import { SubmitButton } from "@/components/submit-button";
import type { InnerSanctumTaskRow } from "@/lib/database.types";
import { INNER_SANCTUM_TASK_STATUSES, taskLabel } from "@/lib/inner-sanctum-tasks";

function localDate(value?: string | null) { if (!value) return ""; const date = new Date(value); return new Date(date.getTime() - date.getTimezoneOffset() * 60000).toISOString().slice(0, 16); }
export function AdminTaskForm({ action, task }: { action: (formData: FormData) => void | Promise<void>; task?: InnerSanctumTaskRow }) {
  return <form action={action} className="form-grid"><label>Slug<input name="slug" pattern="[a-z0-9]+(?:-[a-z0-9]+)*" maxLength={100} required defaultValue={task?.slug ?? ""} /></label><label>Status<select name="status" defaultValue={task?.status ?? "draft"}>{INNER_SANCTUM_TASK_STATUSES.map((status) => <option key={status} value={status}>{taskLabel(status)}</option>)}</select></label><label>Sort order<input name="sort_order" type="number" min="-10000" max="10000" required defaultValue={task?.sort_order ?? 0} /></label><label className="full-span">Eyebrow<input name="eyebrow" maxLength={100} defaultValue={task?.eyebrow ?? ""} /></label><label className="full-span">Title<input name="title" maxLength={200} required defaultValue={task?.title ?? ""} /></label><label className="full-span">Body<textarea name="body" rows={7} maxLength={12000} required defaultValue={task?.body ?? ""} /></label><label className="full-span">Prompt<textarea name="prompt" rows={4} maxLength={2000} required defaultValue={task?.prompt ?? ""} /></label><label>Available from<input name="available_from" type="datetime-local" defaultValue={localDate(task?.available_from)} /></label><label>Closes at<input name="closes_at" type="datetime-local" defaultValue={localDate(task?.closes_at)} /></label><div className="full-span"><SubmitButton>{task ? "Save task" : "Create task"}</SubmitButton></div></form>;
}
