"use client"; import {useState} from "react";
export function PaymentCopyControls({amount,wallet}:{amount:string;wallet:string}){const [copied,setCopied]=useState("");const copy=async(v:string,k:string)=>{await navigator.clipboard.writeText(v);setCopied(k);setTimeout(()=>setCopied(""),1200)};return <div><button type="button" className="button small" onClick={()=>copy(amount,"amount")}>{copied==="amount"?"Copied":"Copy ETH Amount"}</button>{wallet&&<button type="button" className="button small secondary" onClick={()=>copy(wallet,"wallet")}>{copied==="wallet"?"Copied":"Copy Wallet Address"}</button>}</div>}
