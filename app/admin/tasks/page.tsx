import Link from "next/link";
import { requireAdmin } from "@/lib/auth";
import { taskLabel } from "@/lib/inner-sanctum-tasks";

export default async function AdminTasksPage() { const state = await requireAdmin(); if (!state) return null; const { data, error } = await state.supabase.from("inner_sanctum_tasks").select("*").order("sort_order").order("created_at", { ascending: false }); if (error) throw new Error("Tasks could not be loaded."); return <><div className="admin-title"><div><p className="eyebrow">Inner Sanctum</p><h1>Tasks</h1></div><Link className="button" href="/admin/tasks/new">New task</Link></div><section className="admin-panel"><div className="table-wrap"><table><thead><tr><th>Title</th><th>Slug</th><th>Status</th><th>Available</th><th>Closes</th><th>Action</th></tr></thead><tbody>{data?.map((task) => <tr key={task.id}><td><strong>{task.title}</strong></td><td>{task.slug}</td><td><span className={`status ${task.status}`}>{taskLabel(task.status)}</span></td><td>{task.available_from ? new Date(task.available_from).toLocaleString("en-ZA") : "Now"}</td><td>{task.closes_at ? new Date(task.closes_at).toLocaleString("en-ZA") : "Open"}</td><td><Link className="button secondary small" href={`/admin/tasks/${task.id}`}>Edit</Link></td></tr>)}</tbody></table></div></section></>; }
