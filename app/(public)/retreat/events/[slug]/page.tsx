import Link from "next/link";
import { notFound } from "next/navigation";
import { createClient } from "@/lib/supabase/server";
import { formatDate } from "@/lib/domain";
import { retreatDatesFromInclusiveRange } from "@/lib/retreat-dates";
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const supabase=await createClient();const {data:event}=await supabase.rpc("get_public_event_by_slug",{p_slug:slug}).maybeSingle();return {title:event?.title??"Event",description:event?.description??"Filthy Princess fixed-date retreat experience."};}
export default async function EventPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const supabase=await createClient();const {data:event}=await supabase.rpc("get_public_event_by_slug",{p_slug:slug}).maybeSingle();if(!event)notFound();const d=retreatDatesFromInclusiveRange(event.start_date,event.end_date);return <section className="narrow-page"><p className="eyebrow">Fixed-date experience</p><h1>{event.title}</h1><dl className="definition-list"><div><dt>Arrival</dt><dd>{formatDate(d.arrivalDate)}</dd></div><div><dt>Nights</dt><dd>{d.nights}</dd></div><div><dt>Checkout</dt><dd>{formatDate(d.checkoutDate)}</dd></div><div><dt>Places remaining</dt><dd>{event.effective_places_remaining>0?event.effective_places_remaining:"Sold Out"}</dd></div></dl>{event.description&&<p className="lead">{event.description}</p>}{event.effective_places_remaining>0&&<Link className="button" href={`/retreat/events/${event.slug}/enquire`}>Book My Spot</Link>}<p><Link className="text-link" href="/retreat">Back to Retreats</Link></p></section>}
